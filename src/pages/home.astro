---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/NavBar.astro';
import AlertButton from '../components/AlertButton.jsx'; 
import EmergencyMenu from '../components/EmergencyMenu';
import ThemeIcon from '../components/DarkModeToggle.astro';
---

<Layout title='Inicio.'>
  <ThemeIcon />
  <main class="flex justify-center items-center min-h-screen">
    <Navbar />
    <div class="container w-4/5 mt-20 md:mt-0 mb-16">
      <div class="grid grid-cols-1 md:grid-cols-3 md:grid-rows-1 gap-8 items-center justify-center">
        <!-- Boton de Reporte -->
        <a href="/reports" class="bg-blue-500 text-white flex flex-col items-center justify-center w-full h-40 md:h-64 rounded-2xl shadow-2xl transition-all duration-200 hover:shadow-lg">
          <img class="w-16 h-16 md:w-20 md:h-20" src="../assets/icons/report-icon.webp" alt="logo Reporte" />
          <span class="mt-2 md:mt-4">Reportes</span>
        </a>

        <!-- Boton de Alerta -->
        <AlertButton client:load />

        <!-- Boton de Chat -->
        <a href="/chat" class="bg-green-500 text-white flex flex-col items-center justify-center w-full h-40 md:h-64 rounded-2xl shadow-2xl transition-all duration-200 hover:shadow-lg">
          <img class="w-16 h-16 md:w-20 md:h-20" src="../assets/icons/chat-icon.webp" alt="logo Chat" />
          <span class="mt-2 md:mt-4">Chat</span>
        </a>
      </div>
    </div>
    <EmergencyMenu client:load />
  </main>
</Layout>
