<div class="flex items-center justify-center min-h-screen p-4">
  <div class="max-w-md w-full mx-4 p-8 bg-white dark:bg-gray-800 rounded-2xl shadow-md">
    <!-- Logo -->
    <div class="flex justify-center mb-6">
      <img src="/img/reset-password.webp" alt="Logo" class="w-24 h-24">
    </div>

    <h2 class="text-2xl font-semibold text-center text-gray-700 dark:text-gray-300 mb-6">
      Recuperar Contraseña
    </h2>

    <!-- Form -->
    <form id="forgot-password-form" method="POST" action="/api/forgot-password" class="space-y-6">
      <div class="mb-4">
        <label for="email" class="block text-gray-700 dark:text-gray-300 text-sm font-bold mb-2">
          Ingresa tu correo electrónico:
        </label>
        <input
          type="email"
          id="email"
          name="email"
          required
          class="w-full py-2 px-3 border rounded-md bg-gray-100 text-gray-800 dark:bg-gray-700 dark:text-gray-300 shadow-inner focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
      </div>

      <div class="flex justify-center">
        <button
          type="submit"
          class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-full w-full transition-colors duration-300"
        >
          Enviar correo de restablecimiento
        </button>
      </div>
    </form>
  </div>
</div>


<!-- le puse estilos en linea porque se ve que se estan pisando estilos tenggo que revisar bien seguramente tenga que ver con el layout -->
